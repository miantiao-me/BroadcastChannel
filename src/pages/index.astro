---
import List from '../components/list.astro'
import { getTimelinePage } from '../lib/telegram'

const { channel } = await getTimelinePage(Astro)
---

<List channel={channel} after={false} pageHeading={channel.title} />
